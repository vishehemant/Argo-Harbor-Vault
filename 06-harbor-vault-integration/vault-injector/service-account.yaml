# ServiceAccount for the Vault Agent Injector demo.
#
# This ServiceAccount is bound to a Vault Kubernetes auth role
# (created in 02-vault-basics/scripts/setup-kubernetes-auth.sh).

apiVersion: v1
kind: ServiceAccount
metadata:
  name: myapp-sa
  namespace: my-app
  labels:
    app: sample-web
